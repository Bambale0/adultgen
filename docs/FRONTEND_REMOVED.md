# Frontend reset history

The previous `apps/web_app` implementation was removed in PR #56 because it was not acceptable as a product UI.

That reset is complete. A new implementation now lives in `apps/studio_app` and does not reuse the removed source code.

See [`FRONTEND_REBUILD.md`](FRONTEND_REBUILD.md) for the current product surface, architecture, design system, API boundary and verification plan.
